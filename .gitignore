# macOS
.DS_Store

# Large local originals (the optimized versions live in media/)
*.mov
/Screenshot *.png

# Build artifacts — regenerated by build_parkobj.py on each run
_palette_tmp/
_images.dat
_sprite_manifest.json

# Local tooling / backups
.claude/
config.ini.bak
