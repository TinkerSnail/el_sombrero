# macOS
.DS_Store

# Large local originals (the optimized versions live in media/)
*.mov
/Screenshot *.png

# Build artifacts - regenerated by build_parkobj.py on each run
_palette_tmp/
_images.dat
_sprite_manifest.json

# Working scratch (sprite exports, edit backups) - derived, not needed to build
_exported_sprites/
_edit_backup/

# Local tooling / backups
.claude/
config.ini.bak
